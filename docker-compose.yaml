services:
  auth:
    build:
      context: .
      dockerfile: ./apps/auth/Dockerfile
      target: development
    command: pnpm run start:dev auth
    ports :
      - '3000:3000'
    volumes:
     - .:/usr/src/app
  mongo :
    image: mongo
    restart: always
    ports:
      - 27018:27017